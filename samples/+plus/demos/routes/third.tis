class ThirdController: Element {

  function attached() {
    this.msg = this.$(#msg);
    this.msg.text = "ThirdController activated!";
  } 
  
  event click $(button#test) {
    view.msgbox(#information, "Click handled by ThirdController");
  };

}
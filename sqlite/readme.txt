========================================================================
       DYNAMIC LINK LIBRARY : tiscriptsqlite
========================================================================

SQLite3 engine wrapped as TIScript/Sciter extension library.

This is used for demonstration 

   load library "libraryname"; 

feature in tiscript.
